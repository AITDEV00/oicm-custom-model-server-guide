# Pinned to versions verified working on CPU.
# NOTE: silero-vad is intentionally NOT installed — its pip package pulls in
# torch + CUDA (~3-4 GB). We run the bundled .onnx model directly via
# onnxruntime instead; the model is fetched in the Dockerfile.
fastapi==0.136.3
uvicorn[standard]==0.49.0
numpy==2.4.4
onnxruntime==1.24.4
